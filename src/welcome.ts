const projectName: string = 'shoplite-test-foundations';
const readyForAutomation: boolean = false;

console.log(`Welcome to ${projectName}!`);
console.log(`Ready for Playwright automation? ${readyForAutomation}`);
